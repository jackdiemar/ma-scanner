# Copy to an untracked local .env only if your runner loads env files.
# Do not commit real values.
FMP_API_KEY=
SMTP_USER=
SMTP_PASSWORD=
SMTP_RECIPIENT=
SMTP_RECIPIENTS=
SENDER_EMAIL=
SENDER_PASSWORD=
RECIPIENT_EMAIL=
BSC_DASHBOARD_USERS={"admin":"pbkdf2_sha256$260000$salt_hex$digest_hex"}
PORT=8000
